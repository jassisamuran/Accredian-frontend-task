import React from "react";

const SignUpper = ({ data }) => {
  return (
    <div
      style={{
        color: "white",
        background: "gray",
        height: "120px",
        margin: "10px",
        borderRadius: "10px", // Adjust the border-radius as needed
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "24px",
      }}
    >
      Welcome to Accredian
    </div>
  );
};

export default SignUpper;
